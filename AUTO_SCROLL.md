# UI自动滚动功能更新

## 已实现功能

为所有信息流添加了**自动滚动到最新内容**的功能，确保用户始终看到最新信息。

### 1. 主对话区域
- **位置**：游戏主界面的对话消息容器
- **触发**：每次添加新消息时（NPC发言、玩家发言、系统提示）
- **行为**：平滑滚动到底部，显示最新消息
- **代码位置**：`frontend/js/ui.js` - `addMessage()` 方法

### 2. 侧边栏 - 证据标签
- **位置**：侧边栏的"已公开证据"区域
- **触发**：每次更新侧边栏时（新证据公开）
- **行为**：平滑滚动到底部，显示最新公开的证据
- **代码位置**：`frontend/js/ui.js` - `updateSidebar()` 方法

### 3. 侧边栏 - 历史记录标签
- **位置**：侧边栏的"对话历史"区域
- **触发**：每次更新侧边栏时（新对话记录添加）
- **行为**：平滑滚动到底部，显示最新的对话历史
- **代码位置**：`frontend/js/ui.js` - `updateSidebar()` 方法

## 技术实现

使用 `scrollTo()` API 的 `smooth` 行为：

```javascript
container.scrollTo({
    top: container.scrollHeight,
    behavior: 'smooth'
});
```

### 关键特性

1. **平滑滚动**：使用 `behavior: 'smooth'` 提供流畅的视觉体验
2. **延迟执行**：使用 `setTimeout(100ms)` 确保DOM完全渲染后再滚动
3. **用户可控**：用户可随时手动滚动查看历史内容
4. **非强制性**：不会阻止用户向上滚动查看旧消息

## 用户体验

- ✅ **圆桌讨论**：新角色发言时自动滚动到最新对话
- ✅ **证据公开**：新证据公开时自动在侧边栏显示
- ✅ **历史记录**：对话历史自动显示最新内容
- ✅ **完全兼容**：支持用户手动滚动，不会强制锁定位置
